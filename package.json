{
    "name": "aws-role-policy-verification",
    "version": "1.0.0",
    "description": "A simple script to verify AWS role policy JSON files.",
    "main": "verifyJSON.js",
    "scripts": {
        "test": "jest --config /code/internship/remitly/jest.config.js"
    },
    "author": "Bartosz Szarlowicz",
    "license": "MIT",
    "engines": {
        "node": ">=10.0.0"
    },
    "devDependencies": {
        "jest": "^29.7.0"
    },
    "jest": {
        "config": "/jest.config.js"
      }
}
